sudo apt install \
stow \
bspwm \
zsh \
nitrogen \
polybar \
dunst \
rofi \
xtrlock \
flameshot \
htop \
bat \
mpv \
thunderbird \
ffmpegthumbnailer \
atool \
cowsay \
figlet \
lolcat \
# clipmenu
xsel \
libxfixes-dev \
# bolt
fzf \
fasd


#stowit
# starship
curl -fsSL https://starship.rs/install.sh | bash

wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh \
  --directory-prefix /tmp
chmod +x /tmp/Miniconda3-latest-Linux-x86_64.sh
# Silent mode + update
/tmp/Miniconda3-latest-Linux-x86_64.sh -b -u
# Already in .zshrc and .bashrc
# ~/miniconda3/bin/conda init bash
# ~/miniconda3/bin/conda init zsh

pip install ueberzug

sudo add-apt-repository ppa:numix/ppa
sudo apt updatfirae
sudo apt install numix-icon-theme-circle

mkdir -p ~/.local/bin
ln -s /usr/bin/batcat ~/bin/bat

#vim-plug
curl -fLo ~/.local/share/nvim/site/autoload/plug.vim --create-dirs \
pip install pynvim jedi
    https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim

conda create -n nvim3 pip python=3.6; a nvim3; pip install pynvim jedi black; d
conda create -n nvim2 pip python=2.7; a nvim2; pip install pynvim jedi black; d




